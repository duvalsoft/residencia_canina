name: odoo_rpc_example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP & Storage
  http: ^1.1.0
  shared_preferences: ^2.2.3
  cupertino_icons: ^1.0.2
  flutter_secure_storage: ^9.2.2
  
  # Drift para Android nativo
  drift: ^2.14.1
  sqlite3_flutter_libs: ^0.5.20  # ← IMPORTANTE: SQLite nativo para Android
  path_provider: ^2.1.1          # ← IMPORTANTE: Para rutas de archivos
  path: ^1.8.3
  
  # State management & utils
  provider: ^6.1.1
  uuid: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
  # Drift code generation
  drift_dev: ^2.14.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true